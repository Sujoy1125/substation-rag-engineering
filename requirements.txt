openpyxl>=3.1
pandas>=2.0
scikit-learn>=1.3
rank_bm25>=0.2
pytest>=7.4
# fastapi, uvicorn, psycopg[binary], pgvector — add when Phase D (indexing/API) begins
# Phase 4 additions (embeddings — dense retrieval blocked in sandbox, see docs/BLOCKERS.md)
sentence-transformers>=3.0
torch>=2.0
# Generation layer (see docs/GENERATION_LAYER.md). openai is the configured
# default provider; anthropic is optional and only needed if LLM_PROVIDER=anthropic.
# The test suite does not import either — it runs offline against ScriptedClient.
openai>=1.40
