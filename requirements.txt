openpyxl>=3.1
pandas>=2.0
scikit-learn>=1.3
rank_bm25>=0.2
pytest>=7.4
# Service layer (src/api/service.py). The test suite exercises the whole HTTP
# surface offline against ScriptedClient — no API key, no network, no cost.
fastapi>=0.110
uvicorn>=0.29
# psycopg[binary], pgvector — add when dense retrieval is unblocked; the current
# pipeline retrieves in-process over the frozen KB and needs no database.
# Phase 4 additions (embeddings — dense retrieval blocked in sandbox, see docs/BLOCKERS.md)
sentence-transformers>=3.0
torch>=2.0
# Generation layer (see docs/GENERATION_LAYER.md). openai is the configured
# default provider; anthropic is optional and only needed if LLM_PROVIDER=anthropic.
# The test suite does not import either — it runs offline against ScriptedClient.
openai>=1.40
