# Populate before Phase D (indexing) / generation phases
DATABASE_URL=postgresql://user:password@localhost:5432/substation_rag
ANTHROPIC_API_KEY=
# Embedding provider — BLOCKED in current sandbox network policy (see docs/BLOCKERS.md)
EMBEDDING_PROVIDER=
EMBEDDING_API_KEY=
